[project]
name = "portfolio"
version = "0.1.0"
description = "Jacob Gibbons portfolio monorepo (Python workspace root)"
requires-python = ">=3.9"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["backend"]
