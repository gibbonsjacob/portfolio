[project]
name = "portfolio-backend"
version = "0.1.0"
description = "Flask + sqlite3 API for the portfolio contact form"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.1.1",
    "flask-cors>=5.0.1",
]

[tool.uv]
package = false
