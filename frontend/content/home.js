import { COPY_PENDING } from "./copy";

// Home hero strings — fill in, then the page picks them up automatically.
export const home = {
  // One short line under the name
  headline: COPY_PENDING,
  // CTA labels (optional to rewrite)
  ctaWork: "See the work",
  ctaContact: "Say hello",
  // Mono strip under the hero (project ids; not marketing copy)
  fieldProjects: ["fitness-tracker", "songs_db", "automate_life"],
};
